#include "Regex.h"

bool Regex::Parse()
{
	/*
	auto root = parser_.Parse();
	isDFA_ = parser_.isDFA();
	if (isDFA_)
	{
	//	DFAStart_ = root->constructDFA();
		
	}
	else
	{
	//	root->constructNFA();
	}
	*/
	return true;

}